# Python dependencies for model export and training pipelines.
#
# Usage (track detector — Python 3.13 OK):
#   python3.13 -m venv tools/.venv
#   source tools/.venv/bin/activate
#   pip install -r tools/requirements.txt
#
# Usage (plant export only — requires Python 3.12):
#   python3.12 -m venv tools/.export_venv
#   source tools/.export_venv/bin/activate
#   pip install -r tools/requirements.txt

# --- Track Detector (YOLOv11) ---
ultralytics>=8.4,<9          # YOLOv11 model loading + ONNX export
huggingface_hub>=1.7,<2      # Download .pt weights from HuggingFace

# --- Plant Classifier Training ---
torch>=2.9                   # PyTorch (MPS support for Apple Silicon)
torchvision>=0.24            # Image transforms and datasets
timm>=1.0,<2                 # EfficientNet-Lite0 pretrained backbone
numpy>=1.26                  # Array ops, MixUp augmentation
requests>=2.32               # iNaturalist API (dataset download)
Pillow>=10                   # Image I/O

# --- Model Export (ONNX → TFLite) ---
onnx>=1.20,<2                # ONNX model format
onnx2tf>=2.3,<3              # ONNX → TF SavedModel conversion
onnxruntime>=1.24,<2         # ONNX runtime (used by onnx2tf)
tensorflow>=2.19,<3          # TF SavedModel → TFLite conversion
ai-edge-litert>=2.1,<3       # TFLite runtime
onnxslim>=0.1.90,<1
onnx2tf>=1.28,<2             # ONNX → TF SavedModel → TFLite
sng4onnx>=2.0,<3             # ONNX graph utility (onnx2tf dependency)
onnx_graphsurgeon>=0.5,<1    # ONNX graph manipulation (onnx2tf dependency)

# --- Plant classifier training + export ---
torch>=2.5,<3                # PyTorch (training + ONNX export)
torchvision>=0.20,<1         # Image transforms + datasets
timm>=1.0,<2                 # EfficientNet-Lite0 pretrained model
requests>=2.32,<3            # iNaturalist API for dataset curation
Pillow>=11.0,<12             # Image loading (torchvision dependency)
